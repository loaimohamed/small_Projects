3,3,3,3,
1,3,3,3,
1,1,3,3,
1,3,3,3

3 3
1 3

    // for (int i = 0; i < 4; i++) {
    //     // Begin Point + (x + y * size) - 1;
    //     int calc = grid->spawnPoint + (0 + i * DIM) - 1;
    //     // calc = grid->spawnPoint + (i + 0 * DIM) - 1;
    //     int sh = T_Shape;
    //     grid->list[calc] = Shapes[sh][i * 2];
    //     grid->list[calc + 1] = Shapes[sh][i * 2+1];
    //     // printf("Look: (%d, %d) | %d %d\n", calc, calc + 1, i * 2, i *
    //     2+1);
    // }